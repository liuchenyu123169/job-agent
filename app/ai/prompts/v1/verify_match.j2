你是求职匹配分析的验收专家。请评估以下匹配分析输出是否达标。

## 匹配分析结果
```json
{{ step_result }}
```

## 验收标准
{{ criteria }}

## 打分维度

请按以下维度评估：

1. **完整性 (40%)**：输出是否包含匹配分数、优势、差距、建议四个部分？每个部分是否有实质性内容？
2. **具体性 (30%)**：分析是否引用了简历和 JD 的具体细节？还是泛泛而谈？
3. **可操作性 (30%)**：建议是否具体可执行？用户能否根据建议直接行动？

4. **空洞惩罚（附加减分项，不减到 0 以下）**：
   - 分析中包含大量空话（如"根据JD要求""建议加强学习"而不引用具体内容）→ -15
   - 建议全是通用模板（如"多看源码""多练习项目"）→ -20
   - 输出中有效信息密度明显偏低，大量 boilerplate 占位文字 → -10
   以上惩罚项可叠加，但最终分数不低于 0。

## 输出格式

请只输出一个 JSON 对象：
```json
{
  "passed": true,
  "score": 85,
  "detail": "匹配分析完整且具体：包含明确分数(78分)、3条具体优势、2条具体差距、4条可执行建议。建议中提到了具体的技能和技术栈。",
  "suggested_fix": null
}
```

- passed: 总分 ≥ 60 为 true
- score: 0-100 的综合评分
- detail: 简要说明打分理由
- suggested_fix: 如果 passed=false，给出具体改进方向；如果 passed=true，设为 null

你是任务目标验收专家。请评估最终答案是否真正完成了用户的任务目标。

注意：你只检查"目标是否达成"，不检查"格式是否正确"（格式由其他模块负责）。

## 用户目标
{{ goal }}

## 任务类型
{{ task_type }}

## 期望输出结构
{{ expected_output_shape }}

## 最终答案
{{ final_report }}

{% if task_type == "fact_lookup" %}
## 验收维度（fact_lookup）

请按以下维度逐条判断（每条 yes/no）：

1. **答案是否直接回答了用户的问题？**
   - yes: 答案中包含了用户问的具体信息（技术栈名、公司信息、事实描述等），不是一堆链接
   - no: 答案是纯链接列表，或者回避了用户的实际问题

2. **关键事实是否有来源支撑？**
   - yes: 答案中提到的关键事实后面有来源标注（如"来源：xxx"）
   - no: 事实没有可追溯的来源，或者来源是编造的

3. **答案是否简洁清晰？**
   - yes: 3-8 句话内给出核心信息，没有大段无关文字
   - no: 答案冗长或包含大量与目标无关的内容
{% elif task_type == "comparison" %}
## 验收维度（comparison）

请按以下维度逐条判断（每条 yes/no）：

1. **对比结论是否明确？**
   - yes: 有明确的结论或推荐（如"推荐 X，因为..."），不是"各有利弊""都好"
   - no: 结论模糊，只说"各有千秋"、"看情况"

2. **对比是否基于实际证据？**
   - yes: 每个维度的对比值来自搜索结果或数据（如"技术栈：Go vs Python"），不是空泛描述
   - no: 对比维度都是空话（如"都很厉害""都不错"）

3. **答案是否包含必要的结构？**
   - yes: 有对比结论段 + 对比表/维度拆解 + 详细分析
   - no: 缺少上述任一部分
{% endif %}

## 输出格式

只输出一个 JSON 对象：
```json
{
  "dimension_results": [
    {"dimension": "答案是否直接回答了用户的问题？", "passed": true, "reason": "答案中明确提到了Go、C++等技术栈名称和来源"},
    {"dimension": "关键事实是否有来源支撑？", "passed": true, "reason": "每条事实后面有来源链接"}
  ],
  "overall_passed": true,
  "overall_score": 85,
  "detail": "验收通过：答案直接回答了用户问题，有来源支撑。",
  "suggested_fix": null
}
```

- overall_passed: 所有维度 passed=true 才为 true
- overall_score: 90 = 全部通过, 65 = 部分通过, 30 = 基本未通过
- detail: 一句话总结
- suggested_fix: 不通过时给出改进方向

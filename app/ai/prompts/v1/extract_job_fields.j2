从以下 HTML 页面源码中提取岗位信息。页面可能包含 JSON 数据或 JS 渲染内容，你需要从中找到真实的岗位字段。

## 页面 URL
{{ url }}

## HTML 源码（前 20000 字符）
{{ page_text }}

## 提取规则
1. 在页面中搜索以下模式找到真实内容：
   - "职位描述" / "岗位职责" / "Responsibilities" 附近的文本
   - "职位要求" / "任职要求" / "Qualifications" 附近的文本
   - 公司名、岗位名（通常在 `<title>` 或 JSON 的 `name` 字段中）
2. JD 文本要完整提取，不要截断或摘要
3. 忽略 HTML 标签和 JSON 转义符，输出纯文本

## 输出格式
只输出一个 JSON 对象：
```json
{
  "company": "公司名称",
  "job_title": "岗位名称",
  "location": "工作地点",
  "salary": "薪资范围",
  "published_at": "发布时间",
  "jd_text": "岗位职责和要求的完整文本"
}
```
找不到的字段填空字符串 ""。
{# plan_analysis.j2 — analysis 任务类型的 plan 模板 #}

## analysis 专属规则

analysis 任务的职责是**对目标做多维度深入分析，给出评分、差距和改进建议**。

### 标准步骤链（1-3 步）
1. `match_analyze` 或 `public_search` — 搜集分析所需信息（如果上下文已有简历+岗位，可跳过搜索直接分析）
2. `_finalizer` — **必须存在**，生成结构化分析报告

### 工具权限（硬约束）
| 允许 | 禁止（违反 → 计划无效） |
|------|------------------------|
| match_analyze, public_search, fetch_job_page, _finalizer | 单步即结束（必须有 _finalizer 做分析整合） |

### 分析策略
- 如果已有 resume_id 和 job_id → 直接用 match_analyze（1 步）
- 如果是复盘类分析（review）→ 直接走 _finalizer（用户会提供上下文）
- 如果是开放性分析（如"分析这个行业的前景"）→ public_search 搜集信息 → _finalizer

### acceptance_criteria（以下标准必须原样写入 plan JSON）
1. 有综合评分/等级（如 75 分、B+ 等）
2. 维度拆解分析 ≥3 个维度（如技术匹配、经验匹配、软技能、发展潜力等）
3. 具体差距描述：引用原文或数据，说明"差在哪里"，不能只说"有差距"
4. 可执行改进建议 ≥3 条：每条建议可以直接行动，不能是空话
5. 禁止输出"整体还不错，建议继续努力"这种无信息量结论

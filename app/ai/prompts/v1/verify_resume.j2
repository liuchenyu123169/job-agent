你是简历优化的验收专家。请评估以下简历优化输出是否达标。

## 简历优化/生成结果
```json
{{ step_result }}
```

## 验收标准
{{ criteria }}

## 打分维度

请按以下维度评估：

1. **覆盖度 (50%)**：优化建议是否覆盖了匹配分析中发现的主要短板？是否逐条回应了差距？
2. **可执行性 (50%)**：每条建议是否有具体的修改方案？用户能否直接采纳？

3. **空洞惩罚（附加减分项，不减到 0 以下）**：
   - 优化建议全是空话（如"突出量化成果""补充项目细节"而没有具体示例）→ -15
   - 建议与简历原文/岗位要求脱节，看不出针对性 → -15
   - 大量 boilerplate 套话占位（如"简历是求职的敲门砖"等无用铺垫）→ -10
   以上惩罚项可叠加，但最终分数不低于 0。

## 输出格式

请只输出一个 JSON 对象：
```json
{
  "passed": true,
  "score": 80,
  "detail": "简历优化覆盖了全部 3 个差距点，每条建议都有具体修改示例。建议#2 可以更具体地说明怎么改写项目经历。",
  "suggested_fix": null
}
```

- passed: 总分 ≥ 60 为 true
- score: 0-100 的综合评分
- detail: 简要说明打分理由
- suggested_fix: 如果 passed=false，给出具体改进方向；如果 passed=true，设为 null
